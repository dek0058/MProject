#include "UniversalToolkit.h"

//constexpr ulong Seed = static_cast<ulong>(0xUL);

ulong UniversalToolkit::GetHashCode(std::string_view _value)
{
	ulong hash = 0;
    size_t lenth = _value.length();

	

    return hash;
}
