
namespace MProject.Packet;

enum Tag:uint {
    Create          = 0,
    Test            = 1,
    UserLogin       = 2,
    IssueUserKey    = 3
}

table FProtocol {
    tag:uint;
    hash_code:[ubyte];
}

table NProtocolMessage {
    protocol:[FProtocol];
}

root_type NProtocolMessage;
