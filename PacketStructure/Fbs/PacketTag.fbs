
namespace MProject.Packet;

enum Tag:uint {
    Create          = 0,
    Test            = 1,
    
    ClientUserLogin,
    ServerUserLogin,

    // Create
    ClientJoinWorld,
    
    LeftWorld,

    SpawnActor,
    MoveActor,


}

table FProtocol {
    tag:uint;
    hash_code:[ubyte];
}

table NProtocolMessage {
    protocol:[FProtocol];
}

root_type NProtocolMessage;
